package com.example.productcatalog.query.model;

/**
 * Query to retrieve all products currently in stock (quantity &gt; 0).
 * No parameters required.
 */
public class GetInStockProductsQuery {
    // Marker query — no fields needed.
}
